@model aznews.Areas.Admin.Models.HocPhan
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<section class="section">
    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h4 class="fw-bold text-primary text-uppercase mb-4">Cập nhật học phần</h4>

                    <form asp-area="Admin" asp-controller="HocPhan" asp-action="Edit" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="MaHP" />

                        <div class="mb-3">
                            <label class="form-label">Mã số HP</label>
                            <input class="form-control" value="@Model.MaSoHP" disabled />
                        </div>


                        <div class="mb-3">
                            <label class="form-label">Tên học phần</label>
                            <input asp-for="TenHP" class="form-control" />
                            <span asp-validation-for="TenHP" class="text-danger"></span>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label">Số tín chỉ</label>
                                <input asp-for="SoTinChi" type="number" min="1" class="form-control" />
                                <span asp-validation-for="SoTinChi" class="text-danger"></span>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Phân tiết (LT/TH/DA)</label>
                                <input asp-for="PhanTiet" class="form-control" />
                                <span asp-validation-for="PhanTiet" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="form-check form-switch my-3">
                            <input class="form-check-input" type="checkbox" asp-for="TrangThai" />
                            <label class="form-check-label" for="TrangThai">Hoạt động</label>
                        </div>

                        <div class="d-flex gap-2">
                            <button class="btn btn-primary" type="submit"><i class="bi bi-save me-1"></i>Lưu thay đổi</button>
                            <a asp-area="Admin" asp-controller="HocPhan" asp-action="Index" class="btn btn-outline-secondary">Quay lại</a>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
